<!DOCTYPE html>
<html>
	<link type="text/css" rel="stylesheet" href="https://fast.fonts.com/cssapi/96aa841b-f394-4873-b311-7d94a25b1a66.css"/>
	<head>
		<title><%= full_title(yield(:title)) %></title>
		<%= stylesheet_link_tag    "application", :media => "all" %>
		<%= javascript_include_tag "application" %>
		
		
		<%= csrf_meta_tags %>
		<%= metamagic :title => "Neighbor Lending", :description => "Building stronger, more vibrant communities.  A crowdfunding platform with local focus.", :keywords => %w(local capital lending loan crowd source fund funding) %>

		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<%= render 'layouts/shim' %>
	</head>
	<body>
		<%= render 'layouts/header' %>

			<% flash.each do |key, value| %>
				<%= content_tag(:div, value, class: "alert alert-#{key}") %>
			<% end %>			
	  		<%= yield %>
			<%= render 'layouts/footer' %>
			<%= debug(params) if Rails.env.development? %>
			<%= render :partial => ‘layouts/ga’ if RAILS_ENV == ‘production’ %>
	</body>
</html>
